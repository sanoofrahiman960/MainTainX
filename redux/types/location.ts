export interface Location {
    id: string;
    name: string;
    address: string;
    description?: string;
    // Add any other properties your location might have
  }
  export interface Assets {
    id: string;
    name: string;
    description?: string;
    // Add any other properties your location might have
  }